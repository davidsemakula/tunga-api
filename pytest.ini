# -- FILE: pytest.ini (or tox.ini)
[pytest]
DJANGO_SETTINGS_MODULE = tunga.env.test
# -- recommended but optional:
python_files = tests.py test_*.py *_tests.py
addopts = --nomigrations -p no:warnings --cov=. --cov-report html

